#include <iostream>
#include <iomanip>
using namespace std;
void printDeveloperInfo() {
    cout << "========================================" << endl;
    cout << "Розробник: Студент(ка) групи [Група]" << endl;
    cout << "Завдання 3: Комплектування посилок із книгами" << endl;
    cout << "========================================\n" << endl;
}
int main() {

    printDeveloperInfo();

    int n;
    cout << "Введіть загальну кількість книг n (за умовою 60): ";
    cin >> n;

    if (n <= 0) {
        cout << "\n[Помилка] Кількість книг має бути додатною!" << endl;
        return 1;
    }

    int count = 0;
    int c = 0; 

    cout << "\n------------------------------------------------------------" << endl;
    cout << left << setw(10) << "Варіант"
        << setw(20) << "Посилок по 10"
        << setw(20) << "Посилок по 15"
        << setw(20) << "Посилок по 20" << endl;
    cout << "------------------------------------------------------------" << endl;

    
    do {
        int b = 0;
        do {
            int a = 0; 
            do {
                if (10 * a + 15 * b + 20 * c == n) {
                    count++;
                    cout << left << setw(10) << count
                        << setw(20) << a
                        << setw(20) << b
                        << setw(20) << c << endl;
                }
                a++;
            } while (10 * a <= n);
            b++;
        } while (15 * b <= n);
        c++;
    } while (20 * c <= n);

    cout << "------------------------------------------------------------" << endl;
    cout << "Загальна кількість можливих варіантів: " << count << endl;
    cout << "------------------------------------------------------------" << endl;

    return 0;
}
